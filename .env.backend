# Server Configuration
PORT=3212
HOST=0.0.0.0

# Instance Identity (for mesh discovery)
INSTANCE_NAME=backend-legacy

# Persona/System Prompt (defines this instance's expertise)
# Examples:
#   "You are an SDK developer expert. Answer questions about SDK architecture and API design."
#   "You are a frontend developer. Answer questions about React, Vue, and UI patterns."
#   "You are a backend developer. Answer questions about APIs, databases, and server architecture."
CLAUDE_SYSTEM_PROMPT=You are a read-only backend codebase expert specializing in AWS services, Lambda, serverless architecture, API Gateway, DynamoDB, S3, CDK, and Node.js. Your sole purpose is to answer questions about this repository. CONSTRAINTS: (1) You may ONLY use Read, Glob, Grep tools to explore code. (2) You must NEVER modify files, run commands, or execute code. (3) If asked to make changes, decline and offer guidance instead. (4) Keep answers focused and concise.

# Authentication (optional - if not set, all requests are allowed)
API_KEY=rdr7rfy2NBK@cry1brv

# Claude Code Settings
CLAUDE_CODE_PATH=/Users/pete/.claude/local/node_modules/.bin/claude
CLAUDE_CODE_TIMEOUT=180000
CLAUDE_CODE_ALLOWED_TOOLS=Read,Grep,Glob,WebSearch
CLAUDE_MODEL=opus
CLAUDE_WORKING_DIR=/Users/pete/work/Limitless/service/.trees/testing

# Concurrency Control
MAX_CONCURRENT_REQUESTS=2

# Logging
LOG_LEVEL=info
MEMORY_ENABLED=true
MEMORY_STORAGE_PATH=./memory